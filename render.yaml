services:
  - type: web
    name: gst-rag-assistant
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd notebooks && streamlit run streamlit_app.py --server.address=0.0.0.0 --server.port=$PORT
